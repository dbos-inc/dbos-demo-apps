{
  "watch": ["src/","migrations/","views/"],
  "ext": "ts,json,liquid,js",
  "ignore": ["src/**/*.test.ts"],
  "exitcrash": true,
  "exec": "npm run build && npm run db:create && npx knex migrate:latest && npm run start"
}


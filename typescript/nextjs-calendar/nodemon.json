
{
  "watch": ["src/dbos/", "migrations/"],
  "ext": "ts,js,tsx,jsx",
  "ignore": ["src/**/*.test.ts"],
  "events": {
    "restart": "bash -c \"if [[ $FILENAME =~ migrations ]]; then npm run db:setup; fi\"  # TODO Change me if you do not have 'bash'..."
  },
  "exec": "tsc && node dist/src/server.js"
}

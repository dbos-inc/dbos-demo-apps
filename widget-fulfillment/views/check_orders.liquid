<!DOCTYPE html>
<html>
<head>
    <title>Waiting for Orders</title>
    <style>
        body, html {
            margin: 0;
            padding: 0;
            height: 100%;
            background-color: #f5f5f5;
            font-family: Arial, sans-serif;
        }

        .h1 {
          color: #333;
        }

        .form {
          margin-top: 10px;
        }

        .button {
            background-color: #4CAF50;
            border: none;
            color: white;
            padding: 25px 50px;
            text-align: center;
            text-decoration: none;
            font-size: 22px;
            cursor: pointer;
            border-radius: 10px;
            transition: background-color 0.3s, box-shadow 0.3s;
        }

        .button:hover {
            box-shadow: 0 5px 15px 0 rgba(0,0,0,0.2);
        }

        .container {
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 20px;
        }
    </style>
    </head>
<body>
  <h1>Welcome, {{ name }}</h1>
  <p>There are no orders right now.  Waiting for orders...</p>
  <script>
    setTimeout(() => {
      window.location.href = `/fulfill?name={{ name }}`;
    }, 1000);
  </script>
</body>
</html>

name: CI for Java Widget Store

on:
  push:
    branches: [ "main" ]
    paths:
      - 'java/widget-store/**'
      - '.github/workflows/java-widget-store.yml'
  pull_request:
    types:
      - ready_for_review
      - opened
      - reopened
      - synchronize
    paths:
      - 'java/widget-store/**'
      - '.github/workflows/java-widget-store.yml'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Run tests
        working-directory: java/widget-store/
        run: ./gradlew test

name: CI for Rust dbos-toolbox

on:
  push:
    branches: [ "main" ]
    paths:
      - 'rust/dbos-toolbox/**'
      - '.github/workflows/rust-dbos-toolbox.yml'
  pull_request:
    types:
      - ready_for_review
      - opened
      - reopened
      - synchronize
    paths:
      - 'rust/dbos-toolbox/**'
      - '.github/workflows/rust-dbos-toolbox.yml'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: dbos
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - uses: actions/checkout@v5

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Create database
        run: PGPASSWORD=dbos psql -h localhost -U postgres -c "CREATE DATABASE dbos_toolbox_test"

      - name: Build and test
        working-directory: rust/dbos-toolbox/
        run: |
          cargo build --locked
          cargo test --locked
        env:
          PGPASSWORD: dbos
          DBOS_SYSTEM_DATABASE_URL: postgres://postgres:dbos@localhost:5432/dbos_toolbox_test?sslmode=disable

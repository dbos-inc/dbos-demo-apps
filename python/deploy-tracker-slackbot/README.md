# Deploy Tracker Slackbot

This is a Slack bot demo that helps you deploy your app and track the deploy pipeline progress. It listens to commands such as `/deploy` and `/status` in your Slack channel, and sends back status report.

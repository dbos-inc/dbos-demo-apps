services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: dbos
      POSTGRES_DB: interop_dbos_sys
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 2s
      timeout: 5s
      retries: 15
